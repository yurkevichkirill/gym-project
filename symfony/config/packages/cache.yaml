framework:
    cache:
        pools:
            gym.cache:
                adapter: cache.adapter.redis_tag_aware
                provider: 'redis://redis:6379'
                default_lifetime: 3600
