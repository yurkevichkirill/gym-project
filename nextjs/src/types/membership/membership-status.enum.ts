export enum MembershipStatusEnum {
    ACTIVE = 'active',
    EXPIRED = 'expired',
    FROZEN = 'frozen',
    PENDING = 'pending',
    CANCELED_PAYMENT_FAILED = 'canceled_payment_failed',
    CANCELED_BY_CLIENT = 'canceled_by_client',
    CANCELED_BY_SYSTEM = 'canceled_by_system',
}